<% method_list.sort_by {|m| "#{m.scope}#{m.name}" }.each do |meth| %>
<% meth.overloads.each do |overload| %>
<%= overload.visibility == :class ? '+' : '-' %> <%= overload.name(true) %><%= format_args overload %> <%= format_block overload %>

<% end %>
<% end %>
